doctype html
html
    head
        title= title
        meta(name="viewport" content="width=device-width, initial-scale=1")
        meta(charset="UTF-8")
        block scripts
    body(ng-app="app" ng-controller="main")
        block navigation
        block content
        block footer